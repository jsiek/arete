fun inc(!p) {
  *p = *p + 1;
  return *p;
}

fun main() {
  def !q = new 0;
  def p = q; // create a read-only alias, causing q to lose write privilege
  ! inc(q);    // initializing writable pointer requires writable pointer
  *q = *q + 1;
  return *p - *q;
}


