var inc = function !p {
            p[0] = p[0] + 1;
	    return p[0];
          };
var !q = new 0;
var p = q; // create a read-only alias, causing q to lose write privilege
inc(q); // initializing writable pointer requires writable pointer, not ⦅0@1/2⦆
q[0] = q[0] + 1;
return p[0] - q[0];



