var inc = fn write p {
            p := *p + 1;
	    return *p;
          };
var q = write (new 0);
var p = q;
inc(q); // initialing writable pointer requires writable pointer, not ⦅0@1/2⦆
q := *q + 1;
return *p - *q;



