function main() {
  def !x = new (new 10);
  {
    def y = x; // x and y are aliases to the same mutable cell
    **x = 0;   // pointer does not have write privilege
    **y = 1;
  }
  def a = **x - 1;
  return a;
}
