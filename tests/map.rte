var map = function f, ls {
  match ls
  | case ⟨⟩:
    return ⟨⟩;
  | case ⟨hd, tl⟩:
    return ⟨f(hd), map(f, tl)⟩;
};
var ls = ⟨0, ⟨1, ⟨2, ⟨⟩⟩⟩⟩;
var inc = function x { return x + 1; };
var ls2 = map(inc, ls);
return ls2[1][1][0] - 3;
