var t = new ⟨ write ((new 10) ! write) ⟩;
{
  match *t
  | case ⟨ write p ⟩:
    match *t
    | case ⟨ write q ⟩: // init to write privilege requires unique ownership
      p := 0;
      q := 42;
}
return *((*t)[0]);
