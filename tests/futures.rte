fun main() {
  def a = spawn 1 + 2; // 3
  def b = spawn 3 + 4; // 7
  def c = spawn wait a + wait b; // 10
  def d = spawn 5 + 6; // 11
  def e = spawn 7 + 8; // 15
  def f = spawn wait d + wait e; // 26
  return wait c + wait f - 36;
}
